<% layout('layouts/boilerplate.ejs')%>
    <div class="d-flex flex-row justify-content-between">
        <h1 class="col-6">Student Details</h1>
        <div>
            <a href="/students" class="btn btn-primary">Return to All Students</a>
        </div>
    </div>
    <h5>
        Student: <%=student.firstName%>
            <%=student.firstName%>
    </h5>
    <div class="col-2">
        <a href="/students/<%=student._id%>/edit" class="btn btn-info">edit</a>
        <form action="/students/<%=student._id%>?_method=DELETE" method="POST">
            <button class="btn btn-danger btn-sm mt-1">Delete</button>
        </form>
    </div>